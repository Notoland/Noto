# RFC NNNN: Short title

- **Status:** Draft
- **Discussion:** link to the PR

## Summary

One paragraph: what this proposes and why, in terms a user of the language
would recognise.

## Motivation

The problem, with a code example of how it hurts today. If the status quo
is fine, this section will show it and the RFC dies here.

## Proposal

What the language would look like. Code examples carry more weight than
prose — show the syntax, show the diagnostics it produces when used wrong.

## Semantics

The precise rules: typing, evaluation order, interaction with existing
features (`when`, nullability, `defer`, …). Every "unspecified" here is a
bug deferred to the future.

## Alternatives considered

Each alternative and why it loses. An RFC with no alternatives was not
thought about.

## Drawbacks and costs

What gets worse: compile time, binary size, complexity, teaching the
language. Every design makes something worse; name it.

## Unresolved questions

What this RFC deliberately does not decide. Each needs an answer before
`Implemented`.
